# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Generated at runtime -- never committed. fixtures/ IS committed: it is the
# frozen replay snapshot that makes the offline demo and the tests reproducible.
cache/
data/
gold/

# Derivable from fixtures/bt_gtfs.bin by hokieday.gtfs.ensure_extracted()
fixtures/gtfs/

# Local agent/session artifacts -- never commit
.memory/

# Credentials -- never commit
.env*
!.env.example
.databrickscfg
databrickscfg
credentials.json
credentials*.json
token*
